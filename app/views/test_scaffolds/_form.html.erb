<%= form_for(test_scaffold) do |f| %>
  <% if test_scaffold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_scaffold.errors.count, "error") %> prohibited this test_scaffold from being saved:</h2>

      <ul>
      <% test_scaffold.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
